<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('index') }}">{{'home'|trans}}</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				{% for item in cms_menu('main_menu') %}
					<li class="nav-item">
						<a class="nav-link" href="{{ item.url }}">{{ item.label }}</a>
					</li>
				{% else %}
					<li class="nav-item">{{ 'define_menu'|trans }}
						'main_menu'
						{{'items'|trans}}</li>
				{% endfor %}
			</ul>
		</div>
		<div class="collapse navbar-collapse justify-content-end" id="navbarNavLang">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('index', {_locale: 'en'}) }}">EN</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('index', {_locale: 'pl'}) }}">PL</a>
				</li>

			</ul>
		</div>
	</div>
</nav>
